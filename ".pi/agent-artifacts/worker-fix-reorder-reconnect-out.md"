Clean, minimal change. Exactly what was requested.

# Agent Summary

## Status
Done

## Task Completed
Added a pre-disconnect pass in `rebuildCompositingGraph()` that disconnects input 0 from every effect node across all pixel layers before the effect wiring loop runs. This ensures `preserveInlineChainInput()` sees null `currentInput` and falls through to the correct `expectedSource`, preventing stale-connection cycles after effect reorder.

## Files Inspected
- `Gui/Gui05.cpp`: Contains `rebuildCompositingGraph()` and `preserveInlineChainInput()` — the effect wiring loop and the root cause location.

## Files Changed
- `Gui/Gui05.cpp`: Added 13-line disconnect pass (lines 2690–2702) between the `pixelLayers` scan and the main `graphRow` per-layer loop. Disconnects only input 0 (main image input) of effect nodes that have inputs. Does not touch mask inputs or other special inputs.

## Validation
- command: `git diff --check`
- result: passed — no whitespace issues
- command: `cmake --build build --target Natron -j$(nproc)`
- result: passed — clean build, no warnings or errors

## Blockers / Decisions Needed
- None

## Risks / Follow-up
- The disconnect pass runs on every `rebuildCompositingGraph()` call, not just after reorder. This is safe because the wiring loop immediately reconnects everything, but worth noting if performance profiling is ever done on rebuild frequency.