The low-level export guard in Gui05.cpp is also in place.

# Agent Summary

## Status
Done

## Task Completed
Added a `projectSaved` check in `updateUiState()` (Option B) that disables the Run button when the project hasn't been saved, with a tooltip message "Save the project first to enable AI features". All other required guards (Option A — gates in `onRunClicked()`, `runMatAnyone2()`, `runAIPaintLivePreview()`; and the low-level `projectDir.isEmpty()` guard in `exportFluxSam3SourceSequenceForSourceContext()`) were already in place in the committed and/or working tree code.

## Files Inspected
- `Gui/FluxAiPanel.h`: Checked class structure, member variables, method signatures
- `Gui/FluxAiPanel.cpp`: Read `updateUiState()`, `onRunClicked()`, `runAIPaintLivePreview()`, `runMatAnyone2()`, `scheduleAIPaintLivePreview()`
- `Gui/Gui05.cpp`: Read `exportFluxSam3SourceFrameForSourceContext()`, `exportFluxSam3SourceSequenceForSourceContext()`

## Files Changed
- `Gui/FluxAiPanel.cpp`: Added `projectSaved` boolean check in `updateUiState()` that gates the Run button enable state. When project is unsaved but a source is selected and ready, the Run button is disabled and shows a tooltip: "Save the project first to enable AI features".

## Validation
- command: `git diff --check -- Gui/FluxAiPanel.cpp`
- result: passed (no whitespace errors)
- command: `cmake --build build --target Natron -j$(nproc)`
- result: passed — compiled and linked successfully

## Blockers / Decisions Needed
- None

## Risks / Follow-up
- The `onRunClicked()` and `runMatAnyone2()` guards prompt the user with a Save-As dialog before blocking. If the user cancels the dialog, the action is silently aborted with only a log/status message. This is consistent with the existing pattern. Consider whether a more prominent modal warning would be preferred.