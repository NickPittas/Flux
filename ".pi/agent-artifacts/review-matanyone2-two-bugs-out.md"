# Agent Summary

## Status
Done

## Task Completed
Diagnosed both MatAnyone2 workflow runtime failures by tracing actual code paths, reading the SAM3 result manifest, and executing the resolved Python interpreter.

**Bug 1 — Source sequence not reused:** The manifest-check block in `FluxAiPanel.cpp:2698-2713` correctly reads `temporary_source_sequence_dir` from the manifest, resolves frame paths against it, and checks `QFileInfo(firstAbsPath).isFile()`. The manifest stores `temporary_source_sequence_dir: /tmp/Flux/sam3_sequence/44108a3d3f59`, and that `/tmp/` directory no longer exists (confirmed by `ls`). The code therefore correctly falls through to re-export at `FluxAiPanel.cpp:2757`. The manifest does NOT store a persistent project-relative sequence; `Gui05.cpp:943` explicitly uses `QDir::temp()` and `Gui05.cpp:1021` stores only the temp path. **Root cause:** temp files were cleaned between sessions, and the only stored path is the temp dir.

**Bug 2 — Worker reports "matanyone2 not installed":** `FluxAiPanel.cpp:2868-2875` runs `python3 flux_provider_runtime.py python sam3`, which returns `/home/npittas/.local/share/Flux/ai-envs/sam3/venv/bin/python`. The worker is started with that exact Python (`FluxAiPanel.cpp:2894`). That Python currently CAN import `matanyone2`, but the `sam3` venv install metadata (`flux_runtime_install.json`) shows `installed_at: 2026-05-27T10:18:09` and its `packages` list does NOT include `matanyone2` — the venv was created before `matanyone2` was added to `provider_runtime_manifest.json`. The `sam3` self-check script (`sam3_transformers_real_inference_probe.py`) does not verify `matanyone2`, so `flux_provider_runtime.py self-check sam3` misleadingly returned `ready: true` while the package was missing. The user subsequently installed `matanyone2` into the venv manually (package file birth time 2026-05-29 20:20), which is why it works now. **Root cause:** stale venv lacking `matanyone2` at the time of the run, combined with an incomplete self-check that did not catch the missing package.

## Files Inspected
- `Gui/FluxAiPanel.cpp`: manifest-check reuse logic (lines 2685–2720), worker launch in `ensureMatAnyone2WorkerStarted()` (lines 2855–2905)
- `Gui/Gui05.cpp`: `exportFluxSam3SourceSequenceForSourceContext()` temp export and metadata storage (lines 913–1030)
- `tools/ai/matanyone2_worker.py`: `_check_dependencies()` / `load()` (lines 40–55)
- `tools/ai/flux_provider_runtime.py`: Python resolution and self-check behavior
- `/home/npittas/Videos/For_Test/FluxGenerated/AI/base-matte/sam3_transformers/run-20260529-151638/result_manifest.json`: confirmed `temporary_source_sequence_dir` is a `/tmp/` path and `frames` use relative paths
- `/home/npittas/.local/share/Flux/ai-envs/sam3/flux_runtime_install.json`: confirmed venv install date (May 27) and missing `matanyone2` from installed packages list
- `tools/ai/provider_runtime_manifest.json`: confirmed `matanyone2` is now in `sam3` packages list

## Files Changed
None (diagnosis only)

## Validation
- command: `python3 tools/ai/flux_provider_runtime.py python sam3`
- result: passed
- evidence: Returns `/home/npittas/.local/share/Flux/ai-envs/sam3/venv/bin/python` — the C++ uses the correct interpreter.
- command: `/home/npittas/.local/share/Flux/ai-envs/sam3/venv/bin/python -c "import matanyone2"`
- result: passed (currently)
- evidence: Import succeeds, but package file birth time (2026-05-29 20:20) is after the SAM3 run (2026-05-29 15:17 UTC), confirming it was installed after the failure.
- command: `ls /tmp/Flux/sam3_sequence/44108a3d3f59`
- result: failed (directory does not exist)
- evidence: Confirms temp source sequence was cleaned; manifest reuse correctly falls through.
- command: `python3 tools/ai/flux_provider_runtime.py self-check sam3 --json`
- result: passed
- evidence: Returns `ready: true` without checking `matanyone2` — the self-check is incomplete for this worker.

## Blockers / Decisions Needed
None

## Risks / Follow-up
- **Bug 1 fix scope:** Change `Gui05.cpp:943` to export source sequences to a persistent project-relative directory (e.g., inside the run’s `FluxGenerated/AI/...` output folder) instead of `/tmp/`, or add a copy-to-persistent step. Nick should approve the persistent location.
- **Bug 2 fix scope:** Re-run `flux_provider_runtime.py install sam3` to update the venv with the current manifest (which now includes `matanyone2`). Long-term, update the `sam3` self-check script to also verify `matanyone2`, or add a dedicated `matanyone2` runtime with its own self-check.