Only `Gui/FluxAiPanel.cpp` and `Gui/FluxAiPanel.h` were edited in my changes. The `Engine/AIPaint.cpp` and `Engine/AIPaint.h` changes are pre-existing uncommitted changes.

# Agent Summary

## Status
Done

## Task Completed
Fixed two bugs in the MatAnyone2 workflow: (1) `runMatAnyone2()` now checks the selected result manifest for a `sam3_source_sequence` or `matanyone2_source_sequence` with a matching frame range before re-exporting source frames, and (2) `runMatAnyone2()` now sends a `load` command to the MatAnyone2 worker before `infer_video`, with `processMatAnyone2WorkerLine()` auto-sending the pending `infer_video` request upon successful load response.

## Files Inspected
- `Gui/FluxAiPanel.cpp`: Main implementation file for `runMatAnyone2()`, `processMatAnyone2WorkerLine()`, constructor, and request-building logic
- `Gui/FluxAiPanel.h`: Header with member declarations; added `_matAnyone2PendingInferRequest`
- `tools/ai/matanyone2_worker.py`: Read-only; confirmed `load` is required before `infer_video`

## Files Changed
- `Gui/FluxAiPanel.h`: Added `QJsonObject _matAnyone2PendingInferRequest` member
- `Gui/FluxAiPanel.cpp`: (1) Added manifest-check block in `runMatAnyone2()` after `sequenceDir`/`sequenceMetadata` declarations to reuse source sequence from selected result manifest; (2) Changed `runMatAnyone2()` to send `load` first with infer_video stored as pending; (3) Added `load` handler in `processMatAnyone2WorkerLine()` to auto-send pending `infer_video` on success and clear it on failure; (4) Initialized `_matAnyone2PendingInferRequest` in constructor

## Validation
- command: `git diff --check`
- result: passed (no whitespace issues)
- command: `cmake --build build --target Natron -j$(nproc)`
- result: passed (build succeeded, `FluxAiPanel.cpp.o` recompiled and linked)

## Blockers / Decisions Needed
- None

## Risks / Follow-up
- The manifest source-sequence reuse (Fix 1) derives `sequenceDir` from the first frame's absolute directory path. If the manifest stores project-relative paths instead of absolute, the `QFileInfo::isFile()` check would fail and it would fall through to export. This matches the existing cached-sequence check behavior, so it's consistent.