# T083 Stale Packet — DEFERRED / SUPERSEDED

## Status
deferred-pending-viewer-toolbar-multiprompt-recovery

## Original Title
# Planner Report

## Authoritative Nick Commands
Nick's direct commands and the original approved plan override this packet. For T083, SAM3 point/box selection must be in the viewer toolbar, not the AI panel; the viewer must show persistent visual indicators for every point and box; multiple points and boxes per image/video must be supported; add/remove/clear controls belong in the viewer toolbar.

## Why This Packet Is Deferred
This packet was created before the authoritative recovery pass and may preserve AI-panel-owned prompt selection, single-prompt assumptions, source-viewer point-preview shortcuts, or backend/preview work that depends on those invalid assumptions.

Do not implement this packet unless it is rewritten after `tasks/T083-sam3-viewer-toolbar-multiprompt-recovery-plan.md` is implemented and reviewed.

## Preserved Historical Content
The previous contents are intentionally not executable. Use git history if details are needed for forensic reference.
