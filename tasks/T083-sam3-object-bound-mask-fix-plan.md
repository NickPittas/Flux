# T083 Stale Product-Workflow Packet — DEFERRED / SUPERSEDED

## Status
deferred-pending-viewer-toolbar-multiprompt-recovery

## Original Title
# Planner Report

## Authoritative Nick Commands
Nick's direct commands and the original approved plan override this packet. For T083, SAM3 point/box selection must be in the viewer toolbar, not the AI panel; the viewer must show persistent visual indicators for every point and box; multiple points and boxes per image/video must be supported; add/remove/clear controls belong in the viewer toolbar.

## Why This Packet Is Deferred
This packet belongs to the product workflow path and may contain older source-viewer, point-preview, object-bound mask, or probe assumptions that predate the authoritative viewer-toolbar multi-prompt recovery. It must not be used as an executable implementation packet until rewritten after `tasks/T083-sam3-viewer-toolbar-multiprompt-recovery-plan.md` is implemented and reviewed.

## Preserved Historical Content
The previous contents are intentionally not executable. Use git history if forensic detail is needed.
